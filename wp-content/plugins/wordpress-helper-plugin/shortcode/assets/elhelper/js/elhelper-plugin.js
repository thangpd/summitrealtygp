(function ($) {
    $(document).on('ready', function () {
        $('.bhhs-form').on('submit', function (e) {
            e.preventDefault()
            var address = $(this).find('input[name="address"]').val();
            // var nonce = $(this).attr("data-nonce")
            // var data = {action: "search_bhhs_form", post_id: post_id, nonce: nonce};
            var data = {action: "search_bhhs_form", address: address};
            $('.res-search').html('');
            $.ajax({
                type: "GET",
                url: ajax_object.ajax_url,
                data: data,
                dataType: 'JSON',
                success: function (res) {
                    console.log('ok')
                    console.log(res.code)
                    let $res = $('.res-search');
                    var est_home_val = `
      <div class="article article-text normal-box normal-text" id="estimated_values_content">
            <h1>Estimated Home Value        </h1>
      <p>These values are generated by automated computer modeling drawn from public records and should not be considered a definitive statement of this property's worth.</p>
    </div>
                    `
                    var best_val = `<div class="article article-text normal-box normal-text narrow thin-top">
      <h2>The best evaluation comes from a <br> Network real estate professional performing a Comparative Market Analysis (CMA) <br>
It's free, and comes with no obligation</h2>      <div class="row row-button">
                   <a href="/contact" class="btn btn-contact-summit">
			Contact a Summit Realty Group's Agent		
</a>
              </div>
    </div>
`

                    $res.append(res.title)
                    $res.append(est_home_val)
                    $res.append(res.threedots)
                    $res.append(best_val + res.recentby)
                    $res.append(res.soldprice)
                }
            })

        })
    })
})(jQuery);